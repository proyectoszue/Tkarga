<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="view_partner_form_inherit_address_cleanup" model="ir.ui.view">
            <field name="name">res.partner.form.inherit.address.cleanup</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_partner_form"/>
            <field name="arch" type="xml">
                <xpath expr="//div[@class='o_address_format']" position="replace">
                    <div class="o_address_format">
                        <field name="street" placeholder="Street..." class="o_address_street"/>
                        <field name="street2" placeholder="Street 2..." class="o_address_street"/>
                        <field name="country_id" placeholder="Country" class="o_address_country"
                               options="{'no_open': True, 'no_create': True}"/>
                        <field name="state_id" class="o_address_state" placeholder="State"
                               options="{'no_open': True, 'no_quick_create': True}"
                               context="{'country_id': country_id, 'default_country_id': country_id, 'zip': zip}"/>
                        <field name="city" invisible="1"/>
                        <field name="city_id"
                               domain="[('country_id', '=', country_id), ('state_id', '=', state_id)]"
                               context="{'default_country_id': country_id, 'default_state_id': state_id}"
                               placeholder="City" class="o_address_city"/>
                        <field name="z_neighborhood" placeholder="Barrio" class="o_address_city"/>
                        <field name="x_zip_id" placeholder="Código postal" class="o_address_city"/>
                        <field name="zip" placeholder="ZIP" class="o_address_zip" invisible="1"/>
                    </div>
                </xpath>
            </field>
        </record>
    </data>
</odoo>